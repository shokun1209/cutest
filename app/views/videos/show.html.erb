<%= render "shared/header" %>

<div class="show-main">
  <div class="show-photo">
    <%= video_tag(@video.image_url, size:"400x700" ,controls: true,muted: true) %>
  </div>
    <div class="show-btn">
      <div class="destory">
        <%= link_to "削除", video_path(@video.id), method: :delete, data: {confirm: '一度削除すると復旧はできません。 本当に削除しますか？'}, class:"photo-destroy" %> 
      </div>
  </div>
</div>